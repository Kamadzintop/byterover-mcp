# ByteRover MCP Server

MCP (Model Context Protocol) сервер для интеграции ByteRover с Cursor IDE.

## Описание

Этот проект представляет собой MCP-сервер, который позволяет интегрировать возможности ByteRover AI-ассистента в среду разработки Cursor. Сервер работает через stdio протокол и предоставляет инструменты для работы с файлами и анализа кода.

### Смысл работы ByteRover MCP Server

**Основная цель:** Этот сервер расширяет возможности AI в Cursor, давая ему доступ к файловой системе и анализу кода в вашем проекте.

**Без MCP сервера:**
- AI в Cursor может только видеть открытые файлы
- Не может искать файлы в проекте
- Не может анализировать структуру проекта
- Ограничен в понимании контекста

**С MCP сервером:**
- AI может найти любой файл в проекте
- Может проанализировать код и дать рекомендации
- Понимает структуру всего проекта
- Может давать более точные и контекстные советы

### Практические примеры использования

**Сценарий 1: Новый разработчик в проекте**
```
AI: "Покажи мне все основные файлы проекта"
MCP: Находит main.py, config.py, requirements.txt и показывает их

AI: "Объясни структуру проекта"
MCP: Анализирует файлы и объясняет архитектуру
```

**Сценарий 2: Рефакторинг**
```
AI: "Найди все файлы с длинными функциями"
MCP: Ищет файлы с функциями >50 строк

AI: "Проанализируй их на предмет рефакторинга"
MCP: Анализирует каждый файл и дает рекомендации
```

**Сценарий 3: Поиск проблем**
```
AI: "Найди все файлы с тестами"
MCP: Ищет test_*.py, *_test.py файлы

AI: "Проверь качество тестов"
MCP: Анализирует тесты и оценивает их качество
```

### Техническая архитектура

```
Cursor IDE ←→ MCP Protocol ←→ ByteRover Server ←→ File System
     ↓              ↓                ↓              ↓
   AI Assistant  JSON-RPC      Python Scripts   Your Files
```

1. **Cursor** отправляет запрос через MCP протокол
2. **Сервер** получает JSON-RPC запрос
3. **Python скрипты** выполняют поиск/анализ
4. **Результат** возвращается обратно в Cursor

### Уникальность этого сервера

**Простота:**
- Работает через stdio (не нужен веб-сервер)
- Минимальные зависимости
- Легко настраивается

**Функциональность:**
- Умный поиск файлов (по паттернам, именам)
- Анализ кода с рекомендациями
- Статистика файлов (строки, символы, размер)

**Интеграция:**
- Полная поддержка MCP протокола
- Работает с любым AI в Cursor
- Не требует дополнительных настроек

**Итог:** Этот сервер превращает Cursor из простого редактора в **умного помощника**, который понимает структуру вашего проекта, может найти любой файл, анализирует код и дает рекомендации. По сути, это **"глаза и мозг"** для AI, позволяющие ему работать с вашим проектом как опытный разработчик!

## Возможности

- 🔍 **Поиск файлов** - быстрый поиск файлов в проекте
- 📊 **Анализ кода** - AI-анализ кода с различными типами проверок
- 🔧 **Интеграция с Cursor** - полная поддержка MCP протокола через stdio
- 🚀 **Простой запуск** - не требует веб-сервера

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd byterover-mcp
```

2. Установите зависимости:
```bash
pip install python-dotenv==1.0.0
```

3. Создайте файл конфигурации:
```bash
cp env.example .env
```

4. Отредактируйте `.env` файл, добавив необходимые значения.

## Настройка в Cursor IDE

1. Откройте Cursor IDE
2. Перейдите в **Settings** (Ctrl+,)
3. Найдите раздел **MCP** или **Model Context Protocol**
4. Нажмите **Add Server**
5. Введите следующие настройки:
   - **Name**: ByteRover MCP
   - **Command**: `python mcp_server.py`
   - **Working Directory**: путь к папке проекта
6. Сохраните настройки

## Тестирование

Проверьте работу сервера:
```bash
python test_mcp.py
```

## Доступные инструменты

1. **file_search** - Поиск файлов по имени
   - Параметры: `query` (строка)

2. **code_analysis** - Анализ кода
   - Параметры: 
     - `file_path` (строка, обязательный)
     - `analysis_type` (enum: "review", "optimization", "documentation")

## Примеры использования

После настройки MCP-сервера с Cursor, вы можете использовать следующие команды:

### Поиск файлов
```
Найди все Python файлы в проекте
```
```
Поищи файлы с названием "config"
```
```
Покажи все JavaScript файлы в папке src
```

### Анализ кода
```
Проанализируй файл main.py на предмет оптимизации
```
```
Сделай code review для файла utils.py
```
```
Проверь документацию в файле api.py
```

### Комбинированные команды
```
Найди все Python файлы и проанализируй их на предмет рефакторинга
```
```
Поищи файлы с тестами и проверь их качество
```

## Структура проекта

```
byterover-mcp/
├── mcp_server.py        # Основной файл MCP сервера
├── test_mcp.py         # Тестовый скрипт
├── requirements.txt     # Зависимости Python
├── env.example         # Пример конфигурации
├── README.md           # Документация
└── .env                # Конфигурация (создается пользователем)
```

## Устранение неполадок

### Сервер отображается красным
- Проверьте, что команда указана правильно: `python mcp_server.py`
- Убедитесь, что рабочая директория указана корректно
- Попробуйте перезапустить Cursor
- Проверьте логи Cursor на наличие ошибок

### Сервер не запускается
- Убедитесь, что Python установлен и доступен в PATH
- Проверьте, что все зависимости установлены
- Запустите `python test_mcp.py` для диагностики

### Команды не работают
- Перезапустите Cursor
- Проверьте настройки MCP в Cursor
- Убедитесь, что путь к рабочей директории правильный

## Разработка

### Добавление новых инструментов

1. Добавьте описание инструмента в список `self.tools`
2. Создайте функцию-обработчик (например, `new_tool_tool`)
3. Добавьте вызов в `handle_tools_call()`

### Логирование

Сервер использует stderr для логирования. Уровень логирования настраивается через переменную окружения `LOG_LEVEL`.

## Лицензия

MIT License

## Поддержка

При возникновении проблем создайте issue в репозитории проекта. 